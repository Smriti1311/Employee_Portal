const initialState = {
    employeeId : '',
    password : ''
}

const LoginReducer = (state = initialState , action) => {
 switch(action.type){

    default :
        return state;
 }
}

export default LoginReducer;